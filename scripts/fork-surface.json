{
  "_comment": "Upstream-owned files this fork intentionally changes. Anything not listed here fails scripts/check-fork-surface.mjs; the fix is to move the change into one of our own trees, or to add it here deliberately. Budgets are the most this fork may change in one commit — they catch creep, not a single honest edit. maxNetLines is the most one commit may change; maxTotalLines is the most this fork may have changed in total.",
  "entries": [
    {
      "path": "ui/src/index.css",
      "maxNetLines": 30,
      "reason": "Tailwind v4 @source directive for plugin bundles. Plugin UI ships no stylesheet and is styled by the host's compiled Tailwind, and v4 only auto-detects sources under ui/, so without this every utility a plugin invents is silently missing. Cannot live elsewhere: the directive is only read from the CSS entry.",
      "maxTotalLines": 60
    },
    {
      "path": "packages/shared/src/types/plugin.ts",
      "maxNetLines": 40,
      "reason": "Page-slot layout option (full-height), so a plugin page can opt out of the host's default scroll container.",
      "maxTotalLines": 60
    },
    {
      "path": "packages/shared/src/validators/plugin.ts",
      "maxNetLines": 40,
      "reason": "Validator half of the page-slot layout option above.",
      "maxTotalLines": 60
    },
    {
      "path": "packages/shared/src/validators/plugin.test.ts",
      "maxNetLines": 40,
      "reason": "Test for the page-slot layout option above.",
      "maxTotalLines": 80
    },
    {
      "path": "ui/src/pages/PluginPage.tsx",
      "maxNetLines": 40,
      "reason": "Honour the page-slot layout option when rendering a plugin page.",
      "maxTotalLines": 80
    },
    {
      "path": "packages/plugins/plugin-npc-factory/src/worker.ts",
      "maxNetLines": 60,
      "reason": "Bridge from the ontology's business-system events into npc-factory. A plugin package, but not one we own.",
      "maxTotalLines": 120
    },
    {
      "path": "package.json",
      "maxNetLines": 30,
      "reason": "Entry points for the repo gates (test:defenses and friends). A gate nobody can run is not a gate.",
      "maxTotalLines": 100
    },
    {
      "path": "AGENTS.md",
      "maxNetLines": 30,
      "reason": "Points at this fork's own conventions (docs-coolie/*, .agents/skills, doc/plans). AGENTS.md is the one file every AGENTS.md-reading tool loads, so the pointers cannot live in our own tree; they are written as plain paths rather than @imports, because an import is inlined into the system prompt on every request and would be paid on every turn.",
      "maxTotalLines": 30
    },
    {
      "path": "scripts/fork-surface.json",
      "maxNetLines": 200,
      "reason": "This manifest itself, and entirely ours — it does not exist upstream. It sits under scripts/ only because that is where the gate is wired (package.json and check-testing-defenses.mjs call it), and that path prefix is the only reason the gate asks about it. The budget is loose on purpose: the file grows by one entry per upstream file we intentionally change, and that growth is the file working, not creep.",
      "maxTotalLines": 320
    },
    {
      "path": "ui/index.html",
      "maxNetLines": 10,
      "reason": "Marker block for the ICP filing footer, plus lang=\"zh-CN\" for a zh-CN deployment. Markers only — the block stays empty unless an operator configures a filing, so upstream behaviour is unchanged without one. The injection lives on the server because the filing number is deployment config and must not be baked into a UI build.",
      "maxTotalLines": 20
    },
    {
      "path": "server/src/app.ts",
      "maxNetLines": 30,
      "reason": "Serves the optional public landing page at `/` in static mode. It has to be here rather than in our own tree because this file is what owns route order: `/` is claimed before the SPA fallback registers, and `/index.html` and every other route keep returning the app shell. Off unless PAPERCLIP_LANDING_ENABLED is set.",
      "maxTotalLines": 60
    },
    {
      "path": "server/src/ui-branding.ts",
      "maxNetLines": 30,
      "reason": "Fills the ICP footer marker block. This file already owns the one other place the server rewrites its own index.html (favicon and runtime branding), and the escaping helper it used to keep privately now comes from server/src/html-escape.ts so the footer and landing page share one implementation.",
      "maxTotalLines": 60
    },
    {
      "path": "server/src/icp-footer.ts",
      "maxNetLines": 90,
      "reason": "New file: resolves and renders the ICP/公安 filing footer. No upstream counterpart, so there is nothing to reconcile at a merge. Its own tree by origin, but it must sit beside the two callers (ui-branding and landing-page) rather than in a package of its own.",
      "maxTotalLines": 110
    },
    {
      "path": "server/src/html-escape.ts",
      "maxNetLines": 20,
      "reason": "New file: the HTML escaping shared by the injected footer and the landing page. No upstream counterpart. Three callers is why it is a module rather than a copy.",
      "maxTotalLines": 30
    },
    {
      "path": "server/src/landing-page.ts",
      "maxNetLines": 560,
      "reason": "New file: the public landing page. No upstream counterpart. The line count is dominated by two things that are content rather than logic — the built-in catalogue of what this deployment has built, and the inline stylesheet — which is also why the operator can override the catalogue from a JSON file without a rebuild.",
      "maxTotalLines": 640
    },
    {
      "path": "server/src/__tests__/icp-footer.test.ts",
      "maxNetLines": 80,
      "reason": "New file: tests for the filing footer, including the escaping of a filing number. No upstream counterpart.",
      "maxTotalLines": 100
    },
    {
      "path": "server/src/__tests__/landing-page.test.ts",
      "maxNetLines": 220,
      "reason": "New file: tests for the landing page — the enable switch, the catalogue override and its fallbacks, and the refusal to link anything but http(s). No upstream counterpart.",
      "maxTotalLines": 260
    },
    {
      "path": "server/src/__tests__/static-index-html.test.ts",
      "maxNetLines": 45,
      "reason": "Covers the filing footer reaching the app shell, and staying out of it when no filing is configured. Added here rather than in a new file because this suite already owns the SPA fallback HTML that the footer is injected into.",
      "maxTotalLines": 90
    },
    {
      "path": "server/src/config.ts",
      "maxNetLines": 12,
      "reason": "Reads PAPERCLIP_BOOTSTRAP_ADMIN_EMAIL into Config.bootstrapAdminEmail. Env-only rather than also a paperclip.json key: the pin is a property of the deployment an operator sets once, and a file-config key would mean touching the config schema that the CLI and shared packages also compile against.",
      "maxTotalLines": 20
    },
    {
      "path": "server/src/auth/better-auth.ts",
      "maxNetLines": 50,
      "reason": "Two databaseHooks that call the pinned-email bootstrap on user create and on session create. This is the only place that sees a credential sign-up and sign-in as they happen, and it is added conditionally, so an instance without the pin keeps upstream's options object byte for byte.",
      "maxTotalLines": 70
    },
    {
      "path": "server/src/bootstrap-admin-email.ts",
      "maxNetLines": 110,
      "reason": "New file: decides whether a sign-up or sign-in should grant the first instance admin, and reuses server/src/first-admin-claim.ts for the locked, once-only insert rather than writing a second claim path. No upstream counterpart.",
      "maxTotalLines": 110
    },
    {
      "path": "server/src/__tests__/bootstrap-admin-email.test.ts",
      "maxNetLines": 220,
      "reason": "New file: the matching rules, the config parse, and the claim outcomes against a real Postgres (including the negative case — an address the operator did not pin never gets admin). No upstream counterpart.",
      "maxTotalLines": 220
    },
    {
      "path": "server/src/__tests__/better-auth-pinned-bootstrap.integration.test.ts",
      "maxNetLines": 190,
      "reason": "New file: drives the real Better Auth mount so the hooks are proven to fire on sign-up and on sign-in. The sign-in case is the one operators hit (register first, pin after), and it is the only coverage of the session hook's email lookup. No upstream counterpart.",
      "maxTotalLines": 190
    },
    {
      "path": "ui/src/bootstrapSetup.ts",
      "maxNetLines": 10,
      "reason": "Names the environment variable the pending page tells an operator to set, next to the CLI fallback it already exported. One source for both, so the page cannot drift from the variable the server reads.",
      "maxTotalLines": 15
    },
    {
      "path": "ui/src/components/BootstrapPendingPage.tsx",
      "maxNetLines": 35,
      "reason": "The public-instance branch used to offer the host CLI as the way to finish setup, which is not a path a product can ask a customer to take. It now leads with the pinned email and offers sign-in, since signing in is what triggers the grant.",
      "maxTotalLines": 50
    },
    {
      "path": "ui/src/pages/BootstrapSetupUxLab.tsx",
      "maxNetLines": 30,
      "reason": "The design-lab fixture keeps its own copy of the pending screens; leaving it on the old wording would make the lab document a design the app no longer ships.",
      "maxTotalLines": 45
    },
    {
      "path": "ui/src/App.test.tsx",
      "maxNetLines": 20,
      "reason": "The public bootstrap-pending assertions move from 'invite-only' wording to the host-decided one, including the sign-in affordance that the pinned-email path needs. Same intent, current copy.",
      "maxTotalLines": 30
    },
    {
      "path": "screenshots/expo-signin-email.png",
      "maxNetLines": 0,
      "reason": "Screenshot evidence for the Expo sign-in screen. A binary, so it counts zero lines; it is declared only because upstream owns the `screenshots/` directory. New evidence should go under `docs-coolie/` instead, which is ours, rather than growing this entry list one image at a time.",
      "maxTotalLines": 0
    }
  ]
}
