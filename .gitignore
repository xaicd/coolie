node_modules
node_modules/
**/node_modules
**/node_modules/
dist/
dist-preview/
dist-flow-preview/
packages/paperclip-runner/runner/target/
ui/storybook-static/
.env
*.tsbuildinfo
drizzle/meta/
.vite/
coverage/
.DS_Store
._*
.AppleDouble
.LSOverride
data/
.paperclip/
.pnpm-store/
tmp-*
cli/tmp/

# Scratch/seed scripts (but not scripts/ dir)
check-*.mjs
!.github/scripts/check-*.mjs
!.github/scripts/tests/check-*.mjs
!scripts/check-*.mjs
new-agent*.json
newcompany.json
seed-*.mjs
server/check-*.mjs
server/seed-*.mjs
packages/db/seed-*.mjs

# npm publish build artifacts
cli/package.dev.json

# Build artifacts in src directories
server/src/**/*.js
server/src/**/*.js.map
server/src/**/*.d.ts
server/src/**/*.d.ts.map
tmp/
feedback-export-*
diagnostics/

# Editor / tool temp files
*.tmp
.vscode/
.claude/settings.local.json
.paperclip-local/
.paperclip-runtime/
/.idea/
/.agents/

# Doc maintenance cursor
.doc-review-cursor

# Playwright
tests/e2e/test-results/
tests/e2e/playwright-report/
tests/runner-e2e/results/
.env.runner-e2e.local
tests/release-smoke/test-results/
tests/release-smoke/playwright-report/
test-results/issue-detail-perf/
tests/storybook-visual/.cache/
tests/storybook-visual/.snapshots/
tests/storybook-visual/baseline-review/
tests/storybook-visual/test-results/
tests/plugin-ontology-ai/playwright-report/
tests/plugin-ontology-ai/test-results/
tests/storybook-visual/playwright-report/
.superset/
.superpowers/
.claude/worktrees/
.herenow
.vercel/
.env.local
test-results/
screenshots/ds-workbench/

# A crash dump is not an artifact: `core` was 1.3 GB of one here. `coscli` writes
# its log and output beside the checkout, and both are run output, not source.
core
coscli.log
coscli_output/

# Command Code's own state. The shared halves of it — `.commandcode/agents/` and
# `.commandcode/skills/` — are tracked, because they mirror `.claude/agents/` and
# `.claude/skills/`, which this repo already tracks. These three are not shared:
# `settings.json` holds machine-specific permission allows (absolute paths,
# one-off commands), `taste/` is rewritten by the preference learner, and
# `import-report.md` is a one-off migration report.
.commandcode/settings.json
.commandcode/import-report.md
.commandcode/taste/
