# Brief: H5 wave 2 — fill wave 1 gaps (CodeDiff/CMEditor/meta + quota/ontology)

Repo: `~/workspace/xaicd/coolie` (main, AFTER 0.5.1 release lands)
PM: Hermes
Worker: cmd（铁匠撞 model catalog 错误）

## 0. Pre-condition

**WAIT for `bash scripts/release-app.sh` to finish first** (proc_e7ea3fb88189 still running).

Check:
```bash
git log --oneline -3   # expect v0.5.1 release commit at HEAD
curl -fsS https://xrobinai.cn/version.json | head -5   # expect 0.5.1/501
curl -fsS https://dls.xrobinai.cn/coolie/app/0.5.1/coolie-release.apk -I | head -3   # expect 200
```

If any of those fail → STOP, tell boss release failed first.

## 1. Gaps from docs-coolie/feature-acceptance-2026-09-21-wave1.md

READ FIRST:
- docs-coolie/feature-acceptance-2026-09-21-wave1.md (test results)
- docs-coolie/TEST-FAILURES-2026-09-21.md (root causes)

DO NOT touch clients/expo/ (already shipped 0.5.1). DO NOT touch ui/ (paperclip upstream).

## 2. GAP 1: CodeDiffCard h5 version (FAIL #15)

- src: clients/expo/src/components/board-inline/CodeDiffCard.tsx (185 lines)
- target: clients/h5/src/components/board-inline/CodeDiffCard.tsx (≥ 100 lines)
- HTML rendering: `<pre>` + line numbers + CSS classes (+/-/space)
- Syntax highlight: react-syntax-highlighter (npm install, or copy via plain CSS)
- DO NOT use CodeMirror here (that's editor, not diff view)

## 3. GAP 2: CodeMirror editor h5 version (FAIL #16)

- target: clients/h5/src/components/board-inline/CodeMirrorEditor.tsx (≥ 120 lines)
- Web-native: use `@uiw/react-codemirror` or codemirror directly via npm
- No iframe, no WebView — pure web CodeMirror 6

## 4. GAP 3: MvpPreviewCard meta row (FAIL #14 partial)

- target: clients/h5/src/components/board-inline/MvpPreviewCard.tsx (+ 30-50 lines for meta)
- Parse meta JSON from `<preview-mvp meta='{...}'>`, render as key/value rows

## 5. GAP 4: 看额度 h5 panel (FAIL #2)

- target: clients/h5/src/screens/DashboardScreen.tsx (new, ≥ 80 lines)
- fetch GET /api/companies, render "本月支出" cards

## 6. GAP 5: 本体驱动 h5 panel (FAIL #11)

- target: clients/h5/src/screens/OntologyScreen.tsx (new, ≥ 80 lines)
- fetch GET /api/ontology/domains, render 7 domain cards

## 7. Wire into App.tsx

- Add routing for DashboardScreen + OntologyScreen
- Top nav: 汇览 / 员工 / 工坊 / 任务 / 本体

## 8. Commit + push

Commit message: `feat(h5): wave 2 — CodeDiff/CMEditor/meta + quota/ontology panels`
Stay within --max-turns 200. Commit + push.

## 9. 完成后立刻派 wave3

WAIT for THIS to complete, then auto-start wave 3 brief (clients/h5/src/screens/workspace/... more coverage).

## 10. Don't do

- ❌ Don't bump versions (release-app.sh did that)
- ❌ Don't touch clients/expo/
- ❌ Don't touch ui/
- ❌ Don't ask permissions

## 11. Done definition

- 5 gaps closed + 5 new files in clients/h5/src/ + tsc 0 + commit + push
- Re-run scripts/e2e-local.sh after, expect all 11 items PASS or 真红 documented