# Brief: 发 0.5.1 APK（bump version + 重打 + bump version.json + OTA）

Repo: `~/workspace/xaicd/coolie` (main)
PM: Hermes
Worker: cmd（铁匠撞 model catalog 错误，门神唯一可用）

## 0. 任务

老板下令：**「算了我他么的发」= 直接发 0.5.1 APK**。

不走 OTA patch 路线，直接 bump 版本 + 重打 APK + bump version.json + 新 OTA bundle。

## 1. 必跑命令

```bash
export DEVELOPER_DIR=/Library/Developer/CommandLineTools
cd ~/workspace/xaicd/coolie

# 1. 跑 release-app.sh（脚本会自动 bump app.json + 写 CHANGELOG + gradle + coscli + version.json + OTA + commit）
bash scripts/release-app.sh
```

## 2. 关键约束

- 不动 `clients/expo/android/**`（gitignored, prebuild 输出）
- 不动其它任何文件（brief 已锁）
- 必须跑通：
  - bump app.json 0.5.0 → 0.5.1
  - bump android/app/build.gradle versionCode 500 → 501
  - gradle assembleRelease
  - coscli upload APK → cos://gzbucket/coolie/app/0.5.1/coolie-release.apk
  - version.json → 0.5.1 / 501
  - OTA bundle → runtimeVersion 0.5.1
  - CHANGELOG 加 v0.5.1 段
  - commit + push

## 3. 如果脚本出错

- gradle 报错 → 看 BUILD_LOG，找根因修
- coscli 报错 → 检查 ~/.cos.yaml 密钥
- OTA 报错 → 跑 `bash scripts/publish-ota.sh android` 单独发 bundle
- 任何一步失败 → 立刻报掌柜，不硬走

## 4. 跑前预检（必跑）

```bash
git status --short   # 期望空
git log --oneline -3  # 期望 HEAD = 1476c8c6b (INSTALL-GUIDE.md)
curl -fsS https://xrobinai.cn/version.json   # 期望 0.5.0/500（发前）
curl -fsS https://dls.xrobinai.cn/coolie/app/0.5.0/coolie-release.apk -I | head -3   # 期望 200
```

## 5. 跑后验

```bash
# 1. APK 上生产
curl -fsS https://dls.xrobinai.cn/coolie/app/0.5.1/coolie-release.apk -I | head -3
# 期望 200

# 2. version.json
curl -fsS https://xrobinai.cn/version.json
# 期望: { "version": "0.5.1", "versionCode": 501, "downloadUrl": ".../0.5.1/coolie-release.apk", ...}

# 3. OTA manifest runtimeVersion
curl -fsS https://xrobinai.cn/ota/manifest | python3 -c 'import json,sys; m=json.load(sys.stdin); print("runtimeVersion=", m.get("runtimeVersion"))'
# 期望: runtimeVersion= 0.5.1

# 4. main commit
git log --oneline -3
# 期望: HEAD = feat(release): v0.5.1 (含 release-app.sh 自增版本号)
```

## 6. weixin 报掌柜

报告内容（5 行）：
- release-app.sh 跑时（X 分钟）
- APK size（X MB）
- APK 公网 URL: https://dls.xrobinai.cn/coolie/app/0.5.1/coolie-release.apk
- version.json 内容
- commit hash
- 装机链接（发老板可直接下载）

老板下一句拿 APK 装机即可。

## 7. 不要做的事

- ❌ 不要 bump 到 0.6.0 / 1.0 / 任何其它号 —— 必须是 0.5.1
- ❌ 不要碰 `release-app.sh`（门神上次报告这脚本不 bump gradle，但今天就是要 release-app.sh 自跑它内部逻辑；如果脚本不 bump gradle，手动 patch gradle 后跑）
- ❌ 不要重打 0.5.0 APK（覆盖会很糟）
- ❌ 不要碰底层 schema / db / paperclip 上游
- ❌ 不要停 OTA（一起发）

## 8. 完成定义

- APK 公网 200 + version.json 0.5.1 + OTA runtimeVersion 0.5.1 + commit + push
- weixin 报告老板 APK URL

## 9. 注意

- **老板手机卸载了**——发完老板重装 0.5.1 APK 即可（INSTALL-GUIDE.md §1 链接已就绪）
- **不要问老板要不要发**——老板已经说「我他么的发」= 发
- **不要回 PM / 文档**——直接跑 release-app.sh