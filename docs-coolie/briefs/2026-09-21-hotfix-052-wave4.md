# Brief: 0.5.2 hotfix + 装机一键直达（wave 4）

Repo: `~/workspace/xaicd/coolie` (main, AFTER wave 3 lands)
PM: Hermes
Worker: cmd

## 0. Pre-condition

WAIT for wave 3 (proc_fac351f114fb) to commit + push first.

Check:
```bash
git log --oneline -10
# expect: wave 3 commit at HEAD with 5 角色 + DS veto
```

## 1. Boss pain points (read first)

Boss already said "算了我他么的发" because:
- Boss installed 0.5.0 and saw NOTHING new
- Boss installed 0.5.1 (we just shipped) and needs to verify
- The INSTALL-GUIDE.md helps, but no automated post-install verification

## 2. Goals

### 2.1 In-app "What's New" + post-install check

After install, the App should show a clear "What's new in 0.5.1" banner or first-run screen that:
- Lists new features (ChatHome 预览 + 工作空间 4 Tab + 5 角色)
- Confirms OTA is working (show runtimeVersion vs APK version)
- If new features missing → tell user explicitly

### 2.2 E2E check that survives in production

`scripts/e2e-local.sh` already runs 8+1 assertions on h5 + expo replays. Add:
- assertion for ChatHome preview URL loads in iframe (currently #13 PASS, but should be invariant)
- assertion for WorkspaceScreen 4 Tab (currently #17 PASS)
- assertion for Ontology ≥ 7 domains (currently R1 PASS)

These become the "production smoke test" — boss can rerun anytime.

### 2.3 0.5.2 = auto-bump-on-install

When 0.5.2 (or any newer) APK is installed, App should auto-detect and:
- Show updated "What's New"
- Re-run post-install check

## 3. Tasks

### 3.1 Client: post-install check + What's New banner

- `clients/expo/src/screens/WhatsNewScreen.tsx` (new, ≥ 100 lines)
  - Show on first launch of new version (track via SecureStore lastSeenVersion)
  - List new features for current version (read from app.json's extra.releaseNotes or a new release-notes.json)
  - 1 button: "我知道了" → mark seen
  - 1 button: "查看演示" → deep link to WorkspaceScreen + send example prompt to board chat
- Wire into `clients/expo/App.tsx` root navigator (after Splash, before tabs)

### 3.2 Client: expose `WorkspaceScreen` deep link

- `clients/expo/app.json` — already has `scheme: "coolie"` — add deep link routes:
  - `coolie://workspace` → workspace modal
  - `coolie://chat/build` → board chat with seeded "build xxx" prompt
- `clients/expo/App.tsx` — handle `Linking` events

### 3.3 E2E: smoke test additions

- `clients/expo/replays/whats-new.ad` — new replay
  - Open App → expect WhatsNewScreen visible
  - Tap "我知道了" → expect BoardChat visible
  - Tap Workspace → expect 4 Tab Modal
- `scripts/e2e-local.sh` — add WhatsNewScreen visibility assertion

### 3.4 Release: bump to 0.5.2

After client changes land, run `scripts/release-app.sh` to ship 0.5.2. The hotfix is small enough that 0.5.2 is appropriate (semver: 0.5.1 → 0.5.2 = patch / fix).

If wave 3 lands bigger DS-veto changes, then bump to 0.6.0 instead. PM decides at release time.

### 3.5 Docs: update INSTALL-GUIDE.md

- Add "step 0" pre-install check
- Verify 0.5.2 link works after release

## 4. Constraints

- DO NOT touch: `ui/`, any version on app side until release time
- DO NOT bump version until client changes land (run release-app.sh LAST)
- Stay within --max-turns 250

## 5. Verification

- [ ] WhatsNewScreen visible after install
- [ ] Deep link `coolie://workspace` opens WorkspaceScreen
- [ ] e2e-local.sh 11+1 assertions all PASS (R3 still real-red documented)
- [ ] 0.5.2 APK uploaded + version.json + OTA runtimeVersion bumped
- [ ] INSTALL-GUIDE.md updated for 0.5.2
- [ ] weixin report boss: APK URL + verification

## 6. Done definition

All 5 tasks done + tsc 0 + e2e 11+1 + commit + push + 0.5.2 release shipped + boss APK URL sent.

## 7. PM commitment

After this wave completes, the install loop is closed:
1. Boss downloads APK from URL
2. Installs + opens
3. Sees WhatsNewScreen with what's in this version
4. Taps Workspace → sees 4 Tab
5. Boss confirms via weixin

No more "算了我他么的发" cycle.