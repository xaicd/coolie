# Brief: wave 11 — wave7 (3 login bug) + wave8 (6 P0 屏) 合并入 0.5.5

Repo: `~/workspace/xaicd/coolie` (main, AFTER wave 10 lands)
PM: Hermes
Wave: 11

## 0. Boss 决策

老板 09-21:
- "太差劲了" — login 不响应 / WhatsNewScreen 不弹 / 「改用 API Key 登录」按不动
- 模拟器实测发现 3 个真 bug
- 反复想撤回 wave7 但没撤回成功

PM 决定:
- wave7 brief 入库 `d371c935f` 但**从未派活跑过**
- 现在合并 wave7 + wave8 + wave10 = 一次发 0.5.5
- 0.5.5 = appBar + tabBar (wave10) + 6 P0 屏 (wave8) + login 修复 (wave7) + 主题色统一

## 1. 必做

### 1.1 wave7 已识别 3 bug

| Bug | 文件 | 修法 |
|---|---|---|
| WhatsNewScreen 不弹 | `clients/expo/App.tsx` line 669-684 | 把 whatsNewOpen state + shouldShowWhatsNew effect 提到 App.tsx 顶层, 在 Login 渲染前先检查 |
| Login 按钮 disabled 看起来无响应 | `clients/expo/App.tsx` line 566 | useToken 切换时清空 token / email / password, 加 ready 状态提示文案 |
| 「改用 API Key 登录」按不动 | `clients/expo/App.tsx` line 633-639 | 切 useToken 时清空 inputs + hitSlop 8 |

### 1.2 wave8 已交付 6 屏 (commit `6975459fd` 已合并)

- InboxScreen / NotificationsScreen / SearchScreen / RegisterScreen / AgentDetailScreen / TaskDetailScreen

### 1.3 wave10 (跑着) 加 appBar + tabBar + 主题色

## 2. 验证

- [ ] 0.5.5 装机模拟器 (Pixel6):
  - 启动 → WhatsNewScreen 弹 (wave7 修)
  - [我知道了] → Login 页
  - 输入邮箱 + 密码 → 登录按钮 enabled (wave7 修)
  - 点 [改用 API Key 登录] → 切换 input mode + 清空 (wave7 修)
  - 点 [登录] → fetch 触发 → 进主页
  - 主页顶部 appBar (wave10) + 底部 tabBar (wave10)
  - 5 Tab (汇览/任务/+/员工/收件箱) 可点
  - 点 [驾驶舱Web] → 跳 coolieweb:// → 0.6.1 Coolie Web
- [ ] 截图证据入 /tmp/emu-evidence/

## 3. Constraints

- 0.5.2 → 0.5.5 bump version (release-app.sh)
- 不碰 0.6.1-paperclip-web (独立包)
- 不改 server / h5 / ui/
- 1 commit = appBar + tabBar (wave10), 1 commit = login fixes (wave7), 1 commit = release 0.5.5

## 4. Done definition

3 commits + 0.5.5 APK 上生产 + 模拟器装机 6 个截图证据 + commit + push + 装机直链给老板。