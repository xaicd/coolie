# Coolie 移动端原生应用 (Mobile Cockpit) 0-1 全量重构规格说明书
# (Mobile Native App 0-to-1 Ground-Up Rebuild Specification: Super-Shell Architecture)

- **文档编号**: SPEC-COOLIE-MOBILE-001
- **版本**: v3.0.0 (终极全融合·零功能丢失版)
- **创建日期**: 2026-09-26
- **主审架构师**: Principal Product Manager & Chief Architect (DS, FDA & Core SWE)
- **目标工程**: `clients/expo` (React Native / Expo · 包名 `cloud.coolie.app`)
- **关联工程**: `server/` (REST API & Better-Auth), `packages/shared/`, `ui/` (Web 全功能控制台), `packages/plugins/plugin-governance/`

---

## 1. 业务愿景与战略立论 (Vision & Strategic Thesis)

### 1.1 痛点本质反思：拒绝“重复造轮子却丢了 90% 功能”
在以往的移动端建设中，团队犯了典型的**“原生复刻陷阱 (The Native Rewriting Trap)”**：
1. **自定义手写把 Web 端功能丢光了**：
   Web 端（`ui/`）倾注了全部研发心血，拥有极其庞大完备的企业控制面能力：
   - **项目全景**：CMMI G1~G5 门禁会签、5+2 黄金文档基线、RTM 需求双向穿透、SPC 3σ 过程控制、SkyWalking 三态活拓扑、DSH API 契约中心、多分支与工作区运行时；
   - **任务与看板**：全状态流转、子任务依赖、富文本 Markdown、关联产物、执行心跳日志；
   - **自动化与成本**：例行定时调度 (Routines)、实时 Token 成本与模型定价、Agent 技能与环境配置。
   而移动端试图用 React Native 从头重新手写这几百个功能，结果只写了不到 10% 的干瘪列表，**把 Web 端的精华功能全部丢了**！
2. **怪异生硬的“Web全功能”遮羞布**：
   因为自定义手写的原生页严重缺失功能，旧版本在顶栏和各页面硬塞了一个叫 `[Web全功能]` 的按钮。
   这个按钮不仅极其怪异（给用户传达 App 是个残废玩具的负面心智），而且一旦点开，由于没有做好会话共享，还会弹窗要求用户“重新输入账号密码”，或者提示“本机未装独立版，请去下载另一个 APK”，体验极度割裂。

### 1.2 架构破局：超级原生外壳 + 泛在 Web 全功能内核 (Super-Shell Architecture)
参考 **Slack、Linear、Shopify Mobile、GitHub Mobile、Basecamp (Turbo Native)** 的跨端设计精髓：
- **Web 端是业务能力的单一事实来源 (Single Source of Truth)**：
  移动端直接无缝内嵌经过视口优化的 Web 全功能引擎。**Web 端有的功能，手机上 100% 都有，一个都不丢！**
  只要后端与 Web 端新增了功能或插件（例如刚刚发布的 `@paperclipai/plugin-governance`），手机端零代码更新，立即同步具备！
- **原生层只做 Web 做不到的“硬件杀手级超能力”**：
  把移动端真机特权做到极致：**长按按住说话 (ASR 秒级派工)**、**原生相机拍摄电脑屏幕 Bug 上报**、**触感震动反馈 (Haptics)**、**锁屏系统级推送**、**硬件级紧急熔断**。
- **底层建立零感知免密会话桥 (Zero-Reauth Session Bridge)**：
  原生端统一走 Web 登录流，Cookie Jar 与 LocalStorage 全端双向自动同步，**100% 杜绝二次输入密码**。
- **界面彻底纯净：消灭所有“Web全功能”字样**：
  用户在使用 App 时，感知到的就是完整、浑然一体的 Coolie 移动控制面。

---

## 2. 系统服务对象角色与功能自然归位 (Personas & Feature Map)

```
┌──────────────────────────────────────────────────────────────────────────────────┐
│                          Coolie 统一业务领域功能全景                             │
├──────────────────┬───────────────────────────────────────────────────────────────┤
│ 业务领域         │ 在移动端的产品呈现与自然归位 (用户感知完全原生)               │
├──────────────────┼───────────────────────────────────────────────────────────────┤
│ 1. 经营态势大盘  │ 汇览 (Cockpit) ── 老板的战略大盘与决策舱                      │
│    (Boss)        │ • 资金与 Token 实时燃尽大盘 (今日花销、本月预测、超额预警)    │
│                  │ • 阻断卡点快审浮条 (右滑同意 / 左滑驳回，带触感震动)         │
│                  │ • 业务本体态势速览 (4 域 · 128 实体，状态正常 🟢，点击直达)   │
│                  │ • 紧急熔断制动器 (Emergency Kill Switch - 毫秒级防爆停机)     │
├──────────────────┼───────────────────────────────────────────────────────────────┤
│ 2. 调度与派工    │ 中央调度中枢 ([+] / 🎙️) ── 核心高管交互，悬浮大按键          │
│    (Boss / PM)   │ • 长按语音秒级派工 (腾讯云 ASR + 豆包语义提取，3 秒智能建单)   │
│                  │ • 轻触敏捷建单抽屉 (自包含，支持 EARS 验收标准输入)           │
├──────────────────┼───────────────────────────────────────────────────────────────┤
│ 3. 任务推进与看板│ 任务与看板 (Tasks) ── Web 全量任务中心无缝透传                │
│    (SWE / FDSE)  │ • 任务全量看板 (Kanban) 与列表双重视图                        │
│                  │ • 完整任务详情：富文本、子任务链、工作区产物、执行心跳日志    │
│                  │ • 移动端单列 Git Diff 语法高亮对比器 (绿色添加/红色删除)      │
├──────────────────┼───────────────────────────────────────────────────────────────┤
│ 4. 协同与工坊    │ 智能体工坊 (Workshop / Chat) ── Web 实时工坊 + 原生多模态     │
│    (全员协同)    │ • SSE 打字机流式输出与思考过程 (<thinking> / CoT) 折叠展开    │
│                  │ • 原生调起相机拍摄电脑报错、圈选涂鸦后发入工坊                │
│                  │ • 嵌入式交互卡片 (构建进度条、审批气泡、原型卡片)             │
├──────────────────┼───────────────────────────────────────────────────────────────┤
│ 5. 项目与深度治理│ 项目中心 (Projects) ── Web 全量工程与治理插件                 │
│    (DS / FDA)    │ • 项目基本信息、仓库绑定、分支状态与工作区                    │
│                  │ • CMMI G1~G5 质量门禁一票签署                                │
│                  │ • 5+2 黄金文档与 RTM 需求全穿透                               │
│                  │ • SPC 3σ 过程控制与 5-Why 鱼骨根因防退化                      │
│                  │ • SkyWalking + Chaos 三态动态活拓扑演练                       │
│                  │ • DSH/MCP 多协议 API 契约中心与 Mock 沙箱                     │
├──────────────────┼───────────────────────────────────────────────────────────────┤
│ 6. 组织与知识底座│ 组织与资产 (Org & Assets) ── 公司三大核心数字资产全景         │
│    (Admin / All) │ • 🧠 业务本体 (Ontology)：实体网络拓扑、快照审计、安全锁死    │
│                  │ • 👥 数字员工 (Agents)：员工花名册、模型底座、技能树          │
│                  │ • ⏱️ 例行计划 (Routines)：自动化周期调度与执行记录           │
│                  │ • 💰 成本核算 (Costs)：模型与 Token 消耗全景分析              │
└──────────────────┴───────────────────────────────────────────────────────────────┘
```

---

## 3. 全局信息架构：干净纯粹的 5 栏底盘 (5-Tab IA)

**彻底移除任何形如“Web全功能”的独立按钮，还给用户一个干净、高级的生产力工具界面：**

```
+-----------------------------------------------------------------------------------+
|  [🏢 XROA 智研科技 v]        [🟢 12 Agents 运行中]        [🔍 全局搜索]  [🔔 收件箱 (3)]  |
+-----------------------------------------------------------------------------------+
|                                                                                   |
|  Tab 1: 汇览 (Cockpit) ── 原生高管决策大盘                                        |
|  • 资金与 Token 实时燃尽大盘 (今日花销、本月预测、超额预警)                         |
|  • 阻断卡点快审浮条 (右滑同意 / 左滑驳回，带触感震动)                              |
|  • 业务本体数字孪生健康态势卡（点击直达本体域列表与拓扑图）                         |
|  • 紧急熔断制动器 (Emergency Kill Switch - 毫秒级停机防刷爆)                        |
|                                                                                   |
|  Tab 2: 任务 (Tasks) ── Web 全量任务中心无缝透传                                  |
|  • 完整呈现 Web 端任务体系：看板 (Kanban) / 列表视图自由切换                      |
|  • 支持高级标签过滤、父子任务分解、富文本 Markdown 详情与执行日志                   |
|  • 移动端专属单列 Git Diff 语法高亮对比器 (绿色添加/红色删除/行内建议)             |
|                                                                                   |
|  Tab 3: 调度中心 ([+] / 🎙️) ── 核心高管交互，悬浮大按键                           |
|  • 长按按住说话 (Hold-to-Talk)：腾讯云 ASR + 豆包语义提取，3 秒智能建单            |
|  • 轻触敏捷建单：单底抽屉式面板，支持关联项目与 EARS 验收标准输入                  |
|                                                                                   |
|  Tab 4: 工坊 (Chat) ── 协同工坊与实时会话                                         |
|  • SSE 打字机流式响应，智能折叠 Agent 内部推理思考过程 (CoT)                       |
|  • 多模态原生采集：调起真机相机拍摄电脑屏幕报错，圈选涂鸦后发入工坊                |
|  • 内嵌交互式卡片：构建进度、审批请求、交付物沙箱跳转                              |
|                                                                                   |
|  Tab 5: 资产与项目 (Projects & Assets) ── 企业核心资产全景                        |
|  • 📁 项目中心 (Projects)：直达详情，**全量支持 CMMI 质量门禁、三态活拓扑、API 契约**|
|  • 🧠 业务本体 (Ontology)：1800 行原生大屏，实体网络拓扑、快照审计、安全锁死       |
|  • 👥 数字员工 (Agents)：点击直接查看真实详情，配置模型底座与技能装备              |
|  • ⏱️ 例行计划 (Routines) & 💰 成本核算 (Costs)                                   |
|                                                                                   |
+-----------------------------------------------------------------------------------+
|   [ 汇览 Cockpit ]   [ 任务 Tasks ]   [ 🎙️ 派工 ]   [ 工坊 Chat ]   [ 项目/资产 ]   |
+-----------------------------------------------------------------------------------+
```

---

## 4. 关键交互重构与技术实现 (Technical Specifications)

### 4.1 彻底清理所有旧代码中的“怪异 Web 按钮与弹窗”
1. **清理 `AppBar.tsx`**：
   - 彻底删除第 83~100 行的 `[Web全功能]` 按钮；
   - 彻底删除 `CoolieWebFallback` 弹窗（不再有“本机未装 Coolie Web · 内置网页兜底”和“安装独立”）；
   - 顶栏右侧由 `[🔍 搜索]` 和 `[🔔 收件箱]` 优雅占据。
2. **清理 `TasksScreen.tsx`**：
   - 彻底删除编排行中的“Web全功能”与“CMMI门禁”散乱按钮；
   - 将“工坊 / 本体 / 产物”杂乱堆叠图标移除，本体归位到 Tab 5 资产中枢，工坊归位到 Tab 4。
3. **清理 `DashboardScreen.tsx`**：
   - 彻底删除快捷入口中的“Web全功能”图标。

### 4.2 零感知免密认证底座 (Zero-Reauth Session Bridge)
彻底修复由于裸请求导致的重新登录漏洞：
```typescript
/**
 * 规范所有内嵌 Webview 容器的鉴权 URL 生成器。
 * 必须走 exchange 302 附带 Set-Cookie，确保 WebView cookie jar 立刻获得有效会话。
 */
export async function buildAuthenticatedAppUrl(targetPath: string = "/dashboard"): Promise<string> {
  const token = await getWebExchangeToken();
  const cleanPath = targetPath.startsWith("/") ? targetPath : `/${targetPath}`;
  const baseUrl = `${COOLIE_WEB_URL.replace(/\/+$/, "")}${cleanPath}`;
  
  if (!token) {
    return `${baseUrl}?shell=native`;
  }
  
  return `${COOLIE_WEB_URL.replace(/\/+$/, "")}/api/auth/exchange?token=${encodeURIComponent(token)}&next=${encodeURIComponent(baseUrl)}&shell=native`;
}
```

### 4.3 泛在 Web 全功能原生容器外壳 (Universal Native Shell)
在 `clients/expo/src/screens/WebContainerScreen.tsx` 中注入移动视口自适应补丁：
1. **移动视口自适应 (Mobile Viewport CSS/JS Injection)**：
   - 自动隐藏 Web 端冗余的 PC 顶部条（`StandaloneBrowserControls`），由原生 App Shell 接管顶部返回与标题；
   - 适配移动设备安全区（`safe-area-inset-top` / `safe-area-inset-bottom`）；
   - 支持移动端惯性平滑滚动与长按选择文本。
2. **手势与物理返回接管 (Gesture Interception)**：
   - 监听 Android 硬件返回按键与 iOS 屏幕左边缘侧滑返回手势；
   - 若 WebView 内部存在浏览历史，在内部逐级返回；后退到首屏时才退出容器或返回上级 Tab。
3. **双向 JSBridge 协议 (Bidirectional JSBridge)**：
   - Web 页面可通过 `window.ReactNativeWebView.postMessage(JSON.stringify({ action, payload }))` 调起原生功能：
     - `TAKE_PHOTO`：调起真机原生相机拍照，将照片注入 Web 对话框；
     - `TRIGGER_HAPTIC`：触发原生马达触觉震动；
     - `EMERGENCY_STOP`：唤醒原生紧急熔断保护锁。

### 4.4 原生硬件杀手级能力 (Native Superpowers)

#### 1. 悬浮按住说话·秒级语音派工 (Hold-to-Talk Voice Dispatch)
- **按键形式**：底栏中央突出大悬浮按键 `[🎙️]`；
- **交互逻辑**：
  - 手指按下触发微震反馈（`Haptics.impactAsync(Light)`），屏幕弹出录音波行动画；
  - 采集麦克风音频，松手后调用腾讯云/豆包 ASR 接口，2 秒内完成语音转文字；
  - 大模型自动提取任务要素：
    - `title`：任务主旨
    - `priority`：紧急程度（语调或字眼包含“加急/严重”设为 `high`/`critical`）
    - `assignee`：推荐承接智能体
  - 弹出 3 秒自动倒计时确认卡，点击“立即派发”或倒计时结束自动调用 API 创建任务。

#### 2. 原生相机多模态录入 (Native Camera & Image Picker)
- 在协同工坊（Tab 4）输入框左侧增加 `[📷 相机]` 图标；
- 点击直接调用真机原生摄像头（`expo-image-picker` / `expo-camera`）拍摄电脑屏幕上的 Bug 或白屏；
- 内置轻量画笔工具支持红框涂鸦圈选，圈出重点区域后直接注入工坊会话发送给 Agent。

#### 3. 硬件级紧急熔断制动器 (Emergency Kill Switch)
- 置于 Tab 1 汇览大盘右上角；
- 带有红白呼吸闪烁视觉，点击弹出全屏警示弹窗；
- 要求指纹/面容或长按 2 秒二次确认，防止误触；
- 确认后立即调用 `POST /api/companies/{companyId}/emergency-stop`，毫秒级暂停本公司所有正在运行的 Agent 进程并停止计费。

---

## 5. EARS 验收标准与质量门禁 (Acceptance Criteria & Gates)

### 5.1 G1 需求覆盖门禁 (EARS 形式化验收)
- **UBIQUITOUS-01 (全功能无遗漏)**：系统应当在移动端完整呈现 Web 全功能（包含 CMMI G1~G5 质量门禁、5+2 黄金文档、RTM 穿透、三态活拓扑、API 契约中心、Routines 定时例程、成本核算大盘），不得因移动端重写而丢失任何 Web 功能。
- **UBIQUITOUS-02 (零怪异按钮)**：系统界面中**严禁**出现任何形如“Web全功能”、“本机未装 Coolie Web”、“安装独立 APK”的生硬按钮与弹窗。
- **EVENT-01 (免密自动漫游)**：WHEN 用户在移动端完成一次登录后，访问任何项目详情、治理门禁或工坊页面，系统应当通过 `/api/auth/exchange` 自动植入 Session Cookie，**100% 杜绝**弹出二次登录或要求输入密码的界面。
- **EVENT-02 (本体显性直达)**：WHEN 用户进入 Tab 5「项目/资产」或在 Tab 1 大盘点击“业务本体态势卡”，系统应当直接打开 `OntologyDomainListScreen`，完整展示业务本体域、实体网络与锁定状态。
- **STATE-01 (按住说话派工)**：WHILE 用户按住中央 `[🎙️]` 键，系统应当调用真机麦克风录音并展示波纹动画；WHEN 松开按键时，应当在 2.5 秒内完成识别并提供派单倒计时。
- **EVENT-03 (紧急一键停机)**：WHEN 用户触发紧急熔断确认，系统应当在 500ms 内向服务端广播下电指令，并将大盘指示灯置为全红告警状态。

### 5.2 G2/G3 技术合规与代码门禁
- [ ] **TS 编译门禁**：`clients/expo` 目录下执行 `pnpm typecheck` 实现 **0 错误**。
- [ ] **Bundle 打包门禁**：`clients/expo` 目录下执行 `pnpm bundle` 离线打包通过。
- [ ] **设计系统规范**：严格遵循 `DESIGN.md` 暗黑层级（`#08090A` / `#0F1011` / `#191A1B`），严禁纯白文字与刺眼强光。

---

## 6. 四步实施落地路线图 (Phased Implementation Roadmap)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ 第 1 步：铲除怪异按钮与修复免密会话桥 (Immediate Action)                     │
│ • 彻底删除 AppBar / TasksScreen / DashboardScreen 里的 [Web全功能] 按钮     │
│ • 彻底删除 CoolieWebFallback 弹窗与双 App 割裂逻辑                         │
│ • 封装统一的 buildAuthenticatedAppUrl，全量接入 /api/auth/exchange 换票     │
├─────────────────────────────────────────────────────────────────────────────┤
│ 第 2 步：重构底栏 5 导航与显性化资产中心                                   │
│ • 将底栏 Tab 规范固化为：汇览 (Cockpit) / 任务 (Tasks) / 🎙️ 派工 /         │
│   工坊 (Chat) / 项目与资产 (Projects & Assets)                              │
│ • 在 Tab 5 资产中枢中，将 1800 行的业务本体 (Ontology) 与员工名册显性直挂   │
│ • 打通员工列表点击下潜进真实详情页 (AgentDetailScreen) 的断头路             │
├─────────────────────────────────────────────────────────────────────────────┤
│ 第 3 步：项目详情透传与 CMMI 质量治理闭环                                   │
│ • 在项目中心点击任意项目，原生拉起项目全功能外壳，免密加载 Web 全功能标签页  │
│ • 100% 完整操作 CMMI 门禁、三态活拓扑、API 契约中心与工作区代码仓库        │
├─────────────────────────────────────────────────────────────────────────────┤
│ 第 4 步：原生杀手级交互注入与发版验证                                       │
│ • 完善中央悬浮 [🎙️] 长按语音识别 (ASR) 与智能意图建单                     │
│ • 完善大盘右上角紧急熔断防爆制动器                                         │
│ • 运行 pnpm typecheck 与 pnpm bundle 验证离线包编译                         │
└─────────────────────────────────────────────────────────────────────────────┘
```
