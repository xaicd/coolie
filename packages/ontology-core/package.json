{
  "name": "@paperclipai/ontology-core",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "The ontology's host-free core: schema, facts, mapping and query services over the ontology tables.",
  "exports": {
    ".": "./src/index.ts",
    "./*.js": "./src/*.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^24.13.3"
  }
}
